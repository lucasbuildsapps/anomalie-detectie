# Ontwikkel-dependencies (niet nodig op de server / Streamlit Cloud)
-r requirements.txt
pytest>=8.0
pytest-cov>=5.0
ruff>=0.6
hypothesis>=6.100
alembic>=1.13
# Voor de FastAPI-service (api/) en zijn tests
fastapi>=0.110
httpx>=0.27
uvicorn>=0.29
