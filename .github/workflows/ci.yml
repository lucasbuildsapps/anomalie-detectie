name: CI

on:
  push:
    branches: [main, master]
  pull_request:

jobs:
  lint-and-test:
    runs-on: ubuntu-latest
    strategy:
      matrix:
        python-version: ["3.12"]
    steps:
      - uses: actions/checkout@v4

      - uses: actions/setup-python@v5
        with:
          python-version: ${{ matrix.python-version }}
          cache: pip

      - name: Install dependencies
        run: |
          python -m pip install --upgrade pip
          pip install -r requirements-dev.txt

      - name: Lint (ruff)
        run: python -m ruff check .

      - name: Tests + coverage
        run: python -m pytest tests/ --cov --cov-report=term-missing

  docker-build:
    runs-on: ubuntu-latest
    needs: lint-and-test
    steps:
      - uses: actions/checkout@v4

      - name: Build image
        run: docker build -t sentinel:ci .

      # Advisory scan: rapporteert kwetsbaarheden maar blokkeert niet.
      # continue-on-error omdat de action zelf regelmatig faalt op het
      # downloaden van de scanner/DB (GitHub rate limits) — een kapotte
      # download mag de build niet rood maken.
      - name: Scan image (Trivy)
        continue-on-error: true
        uses: aquasecurity/trivy-action@v0.33.1
        with:
          image-ref: sentinel:ci
          severity: HIGH,CRITICAL
          exit-code: "0"
