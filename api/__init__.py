"""FastAPI-service over de analyse-kern (core/).

Dit is de migratie-route weg van Streamlit-only: dezelfde berekeningen,
maar als API die door elke frontend (React, andere tools, notebooks)
geconsumeerd kan worden. Streamlit blijft gewoon werken — beide lagen
delen core/.
"""
